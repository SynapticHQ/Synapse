version: "3.9"

services:
  synapse:
    build: .
    container_name: synapse-orchestrator
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - ./logs:/app/logs
